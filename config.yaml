log:
  disable_sentry: true

db:
  url: db_url

listener:
  addr: :8000

cop:
  disabled: true
  endpoint: "http://..."
  upstream: "http://..."
  service_name: blob-svc
  service_port: 80

aws:
  endpoint: service-endpoint
  force_path_style: true
  access_key: aws-user-access-key
  secret_key: aws-user-secret-key
  bucket: bucket-name
  region: region
  expiration: 1h
  ssldisable: true

doorman:
  service_url: "dorman-url"